[build]
  publish = "."
  functions = "netlify/functions"
  command = "cp backend/resume-data.json netlify/functions/ && cd netlify/functions && npm install"

[build.environment]
  NODE_VERSION = "18"

[functions]
  node_bundler = "esbuild"
  included_files = ["backend/resume-data.json"]
