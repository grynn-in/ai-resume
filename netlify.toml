[build]
  publish = "."
  functions = "netlify/functions"
  command = "cd netlify/functions && npm install"

[build.environment]
  NODE_VERSION = "18"
  PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD = "1"

[functions]
  node_bundler = "esbuild"
  included_files = ["backend/resume-data.json"]
